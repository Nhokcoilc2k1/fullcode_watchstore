import classNames from "classnames/bind";
import styles from './OderDetail.module.scss'


const cx = classNames.bind(styles);

function OrderDetail() {
    return ( 
        <div className={cx('wrapper')}>
 
        </div>
     );
}

export default OrderDetail;